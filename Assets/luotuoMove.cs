using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class luotuoMove : MonoBehaviour
{
    public Text txt1;
    // Start is called before the first frame update
    void Start()
    {
        string a = "asdd";
        txt1.text = a;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
